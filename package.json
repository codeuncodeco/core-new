{
  "name": "codeuncode-core-new",
  "private": true,
  "scripts": {
    "dev:cms": "pnpm --filter cu-core dev",
    "dev:web": "pnpm --filter cu-web dev",
    "build:web": "pnpm --filter cu-web build",
    "prepare": "git config core.hooksPath .githooks"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild",
      "unrs-resolver",
      "workerd"
    ]
  }
}
