// Font Awesome icon name (just the glyph, e.g. "code", "palette", "pen-ruler").
// The style prefix (fa-slab / fa-regular) is applied at render-time by
// `ServiceIcon.astro`, so editors only enter the icon name.
export type IconName = string
